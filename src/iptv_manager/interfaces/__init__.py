"""Interfaces layer: entrypoints (CLI, REST API, scheduler).

This is the only layer allowed to wire concrete infrastructure classes
into application use cases (the composition root / dependency
injection point).
"""
